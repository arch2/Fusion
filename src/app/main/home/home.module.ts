import { NgModule } from '@angular/core';
import { DECLARATIONS, IMPORTS, EXPORTS } from './home.common';

@NgModule({
  declarations: [
    ...DECLARATIONS
  ],
  imports: [
    ...IMPORTS
  ],
  exports: [
    ...EXPORTS
  ]
})
export class HomeModule { }
